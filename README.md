# fox-shell
A simple Unix-like shell implemented in C.

- Learning exercise in system programming, process control, and memory management.

Make sure you have a C compiler installed (`gcc` or `clang`).

### Compile:

```sh
gcc -o fs main.c
